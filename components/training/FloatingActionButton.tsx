import React from 'react';

// This component is no longer used after removing the AI feature.
const FloatingActionButton: React.FC = () => {
    return null;
};

export default FloatingActionButton;
